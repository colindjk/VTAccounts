

// For this test, we're going to test
// PaymentSummarySelector, IndirectSelector, FringeSelector, 

describe('RateSelector', () => {
  it('should select a matching date with the correct `id`', () => {

  })
})

describe('FringeSelector', () => {
  it('should exist for the given account `id`', () => {

  })
})

describe('IndirectSelector', () => {
  it('should exist for the given fund `id`', () => {

  })
})

describe('SalarySelector', () => {
  it('should exist for the given employee `id`', () => {

  })
})

describe('AccountPaymentSelector', () => {

  it('should correctly summarize payments based on rates', () => {

  })
})

